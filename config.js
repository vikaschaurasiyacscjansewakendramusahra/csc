/* Vikas CSC Ask AI configuration
 * Free Smart Mode works without any API key.
 *
 * Optional Gemini test mode:
 * - Never publish a real API key in a public GitHub repository.
 * - For production generative AI, use a server-side /api/chat proxy and
 *   keep the provider key in a server/environment secret.
 */
window.VIKAS_GEMINI_API_KEY = '';
window.VIKAS_GEMINI_MODEL = 'gemini-2.5-flash';
